# Auto-generated !!! Do not edit it manually
# use ci-master https://github.com/metanorma/metanorma-build-scripts
name: ubuntu

on: [push]

jobs:
  test-linux:
    name: Test on Ruby ${{ matrix.ruby }} Ubuntu
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby: [ '2.6', '2.5', '2.4' ]
    steps:
      - uses: actions/checkout@master
      - name: Use Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          architecture: 'x64'
      - name: install dependcnies
        run: sudo apt-get -y --no-install-recommends install libreoffice
      - name: Update gems
        run: |
          gem install bundler -v "~> 2"
          bundle install --jobs 4 --retry 3
      - name: Run specs
        run: |
          bundle exec rake
      - name: Test reverse_adoc binary
        run: ./bin/reverse_adoc -e -o test1 spec/assets/external_images.html
      - name: Assert image extraction - reverse_adoc
        run: rm images/001.gif
      - name: Test w2a binary
        run: ./bin/w2a -e -o test1 spec/assets/external_images.docx
      - name: Assert image extraction - w2a
        run: rm images/001.gif && rm images/002.gif
